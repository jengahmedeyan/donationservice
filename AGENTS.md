# AGENTS.md

This repository's agent guidance lives in **[CLAUDE.md](./CLAUDE.md)** — the single source of
truth for how AI coding agents write code here.

Codex and other `AGENTS.md`-based tools: read `CLAUDE.md`. It is kept model-agnostic on purpose
(architecture rules, conventions, the verify commands, and the security non-negotiables) so the
harness travels across models and tools. Do not duplicate the content here — that would let the
two files drift.
